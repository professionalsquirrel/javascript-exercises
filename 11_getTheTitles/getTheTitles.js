const getTheTitles = function(userArray) {
    //map each element to just the title, into a new array
    return userArray.map(element => element.title);
};

// Do not edit below this line
module.exports = getTheTitles;
